[toolchain]
# 1.89.0 is what `anchor init` (Anchor 1.0.2) generates and the minimum that satisfies
# litesvm 0.10.0 and the solana-3.x dev-dependency crates.
# 1.85.0 was our prior pin; updated during M2 scaffold — see docs/decisions/0001.
channel  = "1.89.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
