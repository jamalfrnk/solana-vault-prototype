[toolchain]
# Pinned above Solana's 1.76.0 MSRV; update after Codespace validation confirms
# compatibility with the Agave v3.1.10 / Anchor 1.0.2 target chain.
channel  = "1.79.0"
components = ["rustfmt", "clippy"]
