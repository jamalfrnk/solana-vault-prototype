[toolchain]
# Minimum version that stabilizes `edition = "2024"`, required by Anchor 1.0.2.
# 1.79.0 was tried first but rejected — see docs/decisions/0001-toolchain-version-pinning.md.
channel  = "1.85.0"
components = ["rustfmt", "clippy"]
